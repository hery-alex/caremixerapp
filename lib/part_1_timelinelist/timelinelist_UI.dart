import 'package:flutter/material.dart';

class TimelinelistUi extends StatelessWidget {
  const TimelinelistUi({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}